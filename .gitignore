# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode / build artifacts (generated by `make convert`)
build/
xcuserdata/
*.xcuserstate
*.xcscmblueprint
DerivedData/
*.ipa
*.xcarchive

# npm (used only to build sentiment.min.js; the bundle is committed)
node_modules/
package.json
package-lock.json

# Browser extension packages (produced by `make chrome` / `make firefox`)
dist/

# Local-only config (personal trigger lists, not for public repo)
extension/config.local.json

# Legacy paths
extension/lib/
LinkedIn De-Elonizer/
